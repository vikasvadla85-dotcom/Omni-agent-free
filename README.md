# Omni-agent-free
An ai agent without tokens
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/support-agent.git
git pull origin main --allow-unrelated-histories
git push -u origin main
